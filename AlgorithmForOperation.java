package com.makakaton.dm.ac.tec.makakaton;


public class AlgorithmForOperation{
    public static void main(String[] args) {
       /* MathOperation math =new MathOperation("Level 2",201,200,300,400);
        Scanner scanner=new Scanner(System.in);
        boolean keepGame=true;
        int expandTime =1;
        if (math.getLevel().equals("Level 2")){
            while (keepGame){
                math.setHowManyTimes(1);

                double result = math.mathLevel2();
                System.out.println(result);
                expandTime++;
                math.setExpandTime(expandTime);
                keepGame=scanner.nextBoolean();
            }
        }*/
    }
}